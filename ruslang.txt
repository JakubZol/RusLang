# hello function in RusLang
функция привет(надпись имя):
    надпись текст <- "привет " ++ имя.
    ответ текст.
конец.

# iterative fibonacci in RusLang
функция фиб(номер н):
  если н=1:
    ответ 0.
  иначе_если н=2:
    ответ 1.
  еще:
    тмп_1 <- 0.
    тмп_2 <- 1.
    для к от 2 до н:
      РЕЗ <- тмп_1 + тмп_2.
      тмп_1 <- тмп_2.
      тмп_2 <- РЕЗ.
     конец.
    ответ РЕЗ.
  конец.
конец.


показ(привет("Сергей")).

для к от 1 до 10:
    если к < 4 или к >= 7:
        показ("фиб ").
        показ(фиб(к)).
    иначе_если к = 9:
        стоп.
    еще:
        показ("к ").
        показ(к).
        продолжать.
    конец.
конец.

